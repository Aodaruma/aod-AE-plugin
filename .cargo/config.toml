[alias]
new-plugin = [
    "generate",
    "--path",
    "templates/plugin",
    "--destination",
    "plugins",
    "--define",
    "repository_plugin=true",
]
# workspace の members を触られたくないならこれも足す（必要なら）
# Add "--no-workspace" to the alias above to skip workspace member updates.
