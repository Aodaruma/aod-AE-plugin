# image-scaler ( AOD_ImageScaler )

Scales layers with selectable interpolation modes and optional reciprocal scaling.

This is the After Effects plugin **AOD_ImageScaler**, which provides the **ImageScaler.aex** plugin file for Adobe After Effects.

## Building the Plugin

See the [main README](../../README.md) for instructions on how to build the plugin.
